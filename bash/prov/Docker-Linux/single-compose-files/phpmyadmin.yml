# WebUI for MariaDB
phpmyadmin:
  hostname: phpmyadmin
  container_name: phpmyadmin
  image: phpmyadmin/phpmyadmin
  restart: always
  links:
    - mariadb:db
  ports:
    - 9002:80
  environment:
    - PMA_HOST=mariadb
    - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}